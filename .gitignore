# ───── Build output ─────
# Everything builds under ~/Offline/headroom/; these only guard against strays.
build/
DerivedData/
*.o
*.swiftmodule
*.dSYM/

# ───── Swift Package Manager (if ever added) ─────
.build/
.swiftpm/

# ───── Xcode (if ever added) ─────
*.xcodeproj/xcuserdata/
*.xcodeproj/project.xcworkspace/xcuserdata/
*.xcworkspace/xcuserdata/
*.xcuserstate

# ───── macOS ─────
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes
.fseventsd
.DocumentRevisions-V100
.TemporaryItems
.VolumeIcon.icns

# ───── Resilio Sync temp/state files (the repo lives in a synced tree) ─────
*.rsls
*.rslsc

# ───── Editors / tooling ─────
*.swp
*~
.vscode/
.idea/

# ───── Claude Code (local, per-machine state: settings, locks, etc.) ─────
.claude/

# ───── Local-only notes (personal / session context) ─────
session-notes.md
important-notes.md

# ───── Design references (local only — contains real usage telemetry) ─────
design/
